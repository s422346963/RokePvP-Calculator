# 洛克王国 PvP 伤害计算器 — 设计文档（Design Spec）

> 本文档基于当前实现（`index.html` + `css/head.css`）整理，描述视觉语言、布局结构、组件规范与交互模式，供维护与二次开发参考。

---

## 1. 产品定位与设计目标

| 维度 | 说明 |
|------|------|
| **产品** | 《洛克王国》PvP 伤害计算器（玩家自制工具） |
| **用户** | 需要快速验伤、配招、看击杀线的对战玩家 |
| **场景** | 对战前/中对照：查精灵、调性格/IV、选技能、读伤害百分比 |
| **形态** | 纯前端单页应用（SPA 式交互，无路由框架） |
| **语气** | 工具向、信息密集、对战友好；轻度游戏化（emoji、属性色、攻防色） |

### 设计原则

1. **攻防一目了然** — 攻击方（暖橙）与防御方（冷蓝）始终成对出现，语义不混淆。
2. **先选后算、即时反馈** — 改 IV / 性格 / 技能 / 倍率后立即重算，减少「点计算」步骤。
3. **三档深度** — 快捷（模式 A）→ 参数（模式 B）→ 完整配招（FULL），同一套视觉语言，不同信息密度。
4. **深色优先、可切换** — 默认深色偏游戏 HUD；支持亮色，偏好写入 `localStorage`。
5. **移动端可用** — ≤700px 纵向堆叠，核心操作不依赖悬停。

---

## 2. 信息架构（IA）

```
┌─────────────────────────────────────────────────────────┐
│ Header：品牌 / 数据来源 / 免责 / 主题切换                  │
├─────────────────────────────────────────────────────────┤
│ 攻防配置区                                                │
│  ┌──────────────┐  ⇄  ┌──────────────┐                 │
│  │ 攻击方面板    │      │ 防御方面板    │                 │
│  │ 搜索·卡片·性格│      │ 搜索·卡片·性格│                 │
│  │ 属性·IV·能级  │      │ 属性·IV·能级  │                 │
│  └──────────────┘      └──────────────┘                 │
├─────────────────────────────────────────────────────────┤
│ 收藏配置（条件显示）                                       │
├─────────────────────────────────────────────────────────┤
│ 技能参数区                                                │
│  [ 模式 A | 模式 B | FULL ]  ← Segmented Tabs           │
│  · 模式内容区（互斥显示）                                   │
│  · 特性双面板 / 倍率 / 星陨 / 减伤                         │
├─────────────────────────────────────────────────────────┤
│ 结果面板（伤害数字 · HP 条 · 拆解 · 公式）                  │
└─────────────────────────────────────────────────────────┘
```

### 页面清单

| 页面 | 文件 | 职责 |
|------|------|------|
| 计算器主页 | `index.html` | 全部核心 UI 与计算 |
| 讨论区 | `discus.html` | Giscus 评论 + 返回主页 |

---

## 3. 视觉语言

### 3.1 设计关键词

**深色战术面板 · 紫调强调 · 攻橙防蓝 · 属性色芯片 · 圆角卡片 HUD**

不是极简 SaaS，而是偏「游戏工具 / 对战 HUD」：卡片层级清晰、数字加粗、标签彩色、结果区有轻微紫渐变高亮。

### 3.2 色彩系统

通过 CSS 变量切换主题（`html[data-theme]` 或根变量；实现以 `css/head.css` 为准）。

#### 暗色（默认）

| Token | 值 | 用途 |
|-------|-----|------|
| `--bg` | `#1a1c2e` | 页面底、输入区底 |
| `--card` | `#242640` | 主卡片 |
| `--card2` | `#2e3052` | 次级卡片 / hover |
| `--accent` | `#7c6bff` | 主操作、选中、焦点环 |
| `--accent2` | `#ff6b9d` | 辅强调（较少使用） |
| `--text` | `#e8eaf0` | 主文字 |
| `--text2` | `#9aa0be` | 次要文字 / 标签 |
| `--border` | `#3a3d5c` | 描边 |
| `--atk-color` | `#ff7043` | 攻击方语义色 |
| `--def-color` | `#42a5f5` | 防御方语义色 |
| `--header-bg` | `linear-gradient(135deg, #2d1b69, #1a1c2e)` | 顶栏 |
| `--result-bg` | `linear-gradient(135deg, card → #2a1a5e)` | 结果面板 |

#### 亮色

| Token | 值 | 用途 |
|-------|-----|------|
| `--bg` | `#f0f2f8` | 页面底 |
| `--card` | `#ffffff` | 主卡片 |
| `--card2` | `#e8edf5` | 次级面 |
| `--accent` | `#5b4de8` | 主强调 |
| `--accent2` | `#e8457a` | 辅强调 |
| `--text` | `#1a1c2e` | 主文字 |
| `--text2` | `#5a6080` | 次要文字 |
| `--border` | `#c8cde0` | 描边 |
| `--atk-color` | `#d44a20` | 攻击方 |
| `--def-color` | `#1a7ac8` | 防御方 |
| `--header-bg` | `linear-gradient(135deg, #4a2fa0, #5b4de8)` | 顶栏 |

#### 语义色（跨主题固定 / 半透明底）

| 语义 | 色值示例 | 使用处 |
|------|----------|--------|
| 攻击方徽章 | `rgba(255,112,67,.25)` + 描边 | `.badge-atk` |
| 防御方徽章 | `rgba(66,165,245,.25)` + 描边 | `.badge-def` |
| 连击 / 能耗高亮 | `#ffca28` | 连击输入、能耗标签 |
| 威力数字 | `--atk-color` | 技能威力 |
| 减伤输入 | `#ff6b6b` | 减伤 % |
| HP 低伤害 | 绿渐变 `#66bb6a → #a5d6a7` | `<25%` |
| HP 中伤害 | 黄渐变 `#ffa726 → #ffcc02` | `25–50%` |
| HP 高伤害 | 红渐变 `#ef5350 → #ff8a80` | `≥50%` |
| 克制 ×2 | `#ff6464` 半透明底 | `.eff-2x` |
| 双重克制 | `#ff3333` | `.eff-4x` |
| 抵抗 | `#80e080` | `.eff-05x` |
| 星陨 | `#ce93d8` | 星陨区块标题 |
| 物攻标签 | `#ef9a9a` | `.fsk-tag.phys` |
| 魔攻标签 | `#ce93d8` | `.fsk-tag.magic` |
| 状态标签 | `#80cbc4` | `.fsk-tag.status` |

#### 18 系属性色（芯片 / 标签）

与游戏图鉴对齐，用于 `type-chip`、`t-{属性}` 类：

| 属性 | 背景 | 文字 |
|------|------|------|
| 普通 | `#9e9e9e` | `#fff` |
| 草 | `#66bb6a` | `#fff` |
| 火 | `#ef5350` | `#fff` |
| 水 | `#42a5f5` | `#fff` |
| 光 | `#ffcc02` | `#333` |
| 地 | `#a1887f` | `#fff` |
| 冰 | `#80deea` | `#333` |
| 龙 | `#7c4dff` | `#fff` |
| 电 | `#ffca28` | `#333` |
| 毒 | `#ab47bc` | `#fff` |
| 虫 | `#9ccc65` | `#fff` |
| 武 | `#ef9a9a` | `#333` |
| 翼 | `#81d4fa` | `#333` |
| 萌 | `#f48fb1` | `#fff` |
| 幽 | `#5c6bc0` | `#fff` |
| 恶 | `#4e342e` / `#6d4c41` | `#fff` |
| 机械 | `#78909c` | `#fff` |
| 幻 | `#ce93d8` | `#333` |

浅色属性文字用深色（`TYPE_TEXT_DARK`），保证对比度。

### 3.3 字体

| 角色 | 字体 | 规格 |
|------|------|------|
| UI 正文 | `'Microsoft YaHei', sans-serif` | 11–14px 常规 / 半粗 |
| 标题 | 同族 | 20px / 700（header h1） |
| 关键数字 | 同族加粗 | 19px 属性值；**36px / 900** 结果伤害 |
| 公式 | `monospace` | 11px，次要色 |

字号阶梯（桌面）：

- 8–10px：IV 标签、倍率框标签、表头
- 11–12px：字段标签、徽章、说明
- 13–14px：输入、列表项、面板标题
- 15–16px：精灵名、能级数值
- 19px：属性最终值
- 36px：结果伤害大数字

### 3.4 圆角与阴影

| 元素 | 圆角 | 阴影 / 其它 |
|------|------|-------------|
| 主面板 / 结果 | 12px | 结果有 accent 描边 |
| 次级卡片 / 精灵卡 | 10px | — |
| 输入 / 小按钮 | 6–8px | focus：`border-color: accent` |
| 胶囊徽章 / 主题开关 | 20px | — |
| 交换按钮 | 50% 圆形 | `0 2px 8px rgba(0,0,0,.4)`；hover 旋转 180° |
| 下拉 | 8px | `0 8px 24px rgba(0,0,0,.2~0.25)` |
| 选中 Tab | 8px | `0 2px 8px rgba(124,107,255,.4)` |

### 3.5 间距与布局尺度

| Token 概念 | 值 |
|------------|-----|
| 内容最大宽 | `1200px`，水平居中 |
| 主区内边距 | 桌面 `20px 16px`；移动 `12px 10px` |
| 面板内边距 | `16px`（移动 `12px`） |
| 网格间距 | 面板 `16px`；属性格 `6px`；倍率格 `8px` |
| 攻防列 | `1fr 48px 1fr`（中间为交换列） |

---

## 4. 布局规范

### 4.1 Header

- 横向 Flex：图标 ⚔️ → 标题区（标题链接 + 数据来源 + 免责）→ 右侧主题切换
- 背景：紫系渐变；标题白字
- 移动端：隐藏次要 span，缩小 h1 与 padding

### 4.2 攻防双栏（`.panels`）

```
Desktop:  [ 攻击方面板 ]  [ ⇄ ]  [ 防御方面板 ]
Mobile:   [ 攻击方面板 ]
          [ ⇄ 居中 ]
          [ 防御方面板 ]
```

每侧结构（自上而下）：

1. **Panel Header** — 徽章 + 副标题  
2. **Search Combobox** — 🔍 输入 + 下拉列表（缩略图 + 名 + 形态 + 属性标签）  
3. **Spirit Card** — 64px 头像 + 名称 + 形态/特性 + 属性芯片  
4. **性格选择** — 全宽 select（按增益分组 optgroup）  
5. **Stats Grid** — 3 列 × 2 行：物攻/魔攻/速 + HP/物防/魔防；每格含最终值、种族值、IV 输入  
6. **能力等级行** — `−` / 数值 / `+` + 倍率文案  

**选中态：**

- 攻方当前用于出招的属性格：`.active-atk`（橙边 + 浅橙底）
- 防方当前用于承伤的属性格：`.active-def`（蓝边 + 浅蓝底）

### 4.3 技能参数区（`.skill-panel`）

- 标题：`⚡ 技能参数`
- **Segmented Control**（`.mode-tabs`）：三 Tab 等分；active = 实心 accent + 白字
- 每个 Tab 可带 `.tab-sub` 一行说明（10px）
- 内容区 `.mode-section`：仅一个 `.active` 显示

#### 模式 A — 单技能快捷

- 威力、连击、物/魔切换、属性 chip 选择
- 提示条（紫浅底）
- STAB 覆盖勾选
- 星陨层数、减伤 %（`.reduce-row`）

#### 模式 B — 单技能参数

- 技能库 combobox + 已选信息
- 手动：基础威力、特性双面板、额外倍率、连击
- 自动：克制 / STAB / 攻防能级倍率（4 列 `mult-grid`）
- 星陨、减伤

#### 模式 FULL — 完整技能配置

- 左右两列 `.full-skill-panel`：攻 4 槽 / 防 4 槽  
- 每槽 Grid：`技能 | 类型 | 属性 | 能耗 | 连击 | 威力 | 备注?`  
- 特性双面板 + 额外倍率 / 星陨 / 减伤  
- 内嵌 `full-result-panel`：左右对比条 + 点击技能看明细  

### 4.4 结果面板（`.result-panel`）

- 边框使用 `--accent`，背景轻微紫渐变，与普通卡片区分「产出区」
- 结构建议：
  1. 对战双方文案 + 模式 Tag  
  2. **大数字伤害** + 「伤害」+ 效果 Tag（克制/本系/减伤）  
  3. HP 百分比条（绿/黄/红）+ 文案（一击 / 半血 / 四分之一…）  
  4. 拆解 chips（`.breakdown-item`）  
  5. 可选星陨追加区块  
  6. 公式行（`.formula-row` monospace）

---

## 5. 组件库（实现对照）

### 5.1 徽章 Badge

| 类名 | 样式要点 |
|------|----------|
| `.panel-badge.badge-atk` | 暖橙半透明底 + 描边，12px / 700，圆角 20px |
| `.panel-badge.badge-def` | 冷蓝半透明底 + 描边 |

### 5.2 搜索下拉 Combobox

- 容器 `.search-wrap` 相对定位  
- 输入左图标、右箭头  
- `.dropdown.open` 展开；项含缩略图 28px、名称、形态、属性 tag  
- 滚动条宽 4px  

### 5.3 精灵卡 Spirit Card

- 横向：头像 + 信息  
- 空状态 `.no-spirit` 居中次要色文案  

### 5.4 属性格 Stat Box

- 左：标签 / 大数值 / 种族值  
- 右：IV 窄输入（约 38px 宽）  
- 可点击切换物攻/魔攻或物防/魔防  

### 5.5 能级 Level Stepper

- 背景条 `.level-row`  
- 方按钮 26×26；hover 填 accent  
- 中间数值用 accent 色  

### 5.6 模式 Tab

- 容器 inset 背景 + 4px 内边距  
- 未选：透明底 + text2  
- 选中：accent 实底 + 阴影  

### 5.7 物魔切换 Atk Toggle

- 两枚按钮并排  
- `.active` = accent 实底白字  

### 5.8 属性选择 Type Selector

- 流式 chip 按钮  
- 选中：accent 填充；未选：边框 + text2  

### 5.9 倍率盒 Mult Box

- 小标签 + 居中加粗数字输入  
- 语义色可写在 input 的 inline color（克制红、STAB 金、能级紫/蓝）  

### 5.10 特性面板 Trait Panel

- 双列 Grid；移动单列  
- 攻方 header 浅橙底；防方 header 浅蓝底  
- 名称加粗语义色；描述 11px  
- 倍率输入 + 重置按钮  
- 条件区 `.trait-cond-area` 空则隐藏  
- 提示 `.trait-hint-box` 浅黄底  

### 5.11 技能槽行 FSK Row（FULL）

- CSS Grid 固定列宽（技能栏 flex 吃剩余）  
- hover 浅底；`.selected` accent 边  
- 物/魔/状态 tag 不同色边  
- 备注 `?` 图标 hover 出 tooltip  

### 5.12 结果对比条 FRS Row

- 名称 + 细进度条 + 百分比  
- 选中高亮同 accent  

### 5.13 收藏 Fav

- 分组 header 浅紫底  
- 项：名称 + meta + 删除（hover 变红）  
- 保存按钮：accent 描边 + 浅紫底  

### 5.14 主题开关 Theme Toggle

- 胶囊形、半透明白边  
- 图标 🌙/☀️ + 文案「夜间/日间」  

### 5.15 交换按钮 Swap

- 圆形紫渐变；hover 旋转 180° + 阴影加深  

---

## 6. 交互与状态

### 6.1 全局状态模型（逻辑）

```
state = {
  atk, def,                 // 精灵对象
  atkStat, defStat,         // 'patk'|'matk' / 'pdef'|'mdef'
  atkAtkLevel, defDefLevel, // 能力等级
  skillType, skillAttr,
  mode: 'A' | 'B' | 'FULL',
  selectedSkillName
}
fullState = { atk: [4 slots], def: [4 slots] }
theme → localStorage['theme']
```

### 6.2 关键交互

| 交互 | 行为 |
|------|------|
| 搜索精灵 | 中文 / 拼音 / 首字母过滤；点选填充卡片并 `recalcStats` + `calc` |
| 改性格 / IV | 即时重算属性与伤害 |
| 点属性格 | 切换物/魔攻或物/魔防参与公式 |
| ± 能级 | 更新倍率文案并重算（模式 A 可不吃能级，见逻辑） |
| 切换模式 Tab | 显示对应 section；FULL 走 `calcFull` |
| 技能库选择 | 回填威力、类型、属性、提示 |
| 特性条件输入 | 动态出现输入框，自动/半自动改倍率 |
| 改任意倍率/星陨/减伤 | `oninput` → `calc()` / `calcFull()` |
| 交换攻防 | 交换精灵与相关 UI 状态 |
| 主题 | 切换 `data-theme`，持久化 |

### 6.3 反馈层级

1. **即时数值** — 属性格、伤害大数字  
2. **视觉选中** — 边框/底色/active Tab  
3. **进度条** — HP 占比 0.3s width 过渡  
4. **标签** — 克制、本系、一击提示  
5. **公式展开** — 可审计的计算过程  
6. **Tooltip** — 特殊技能备注（FULL）  

### 6.4 空 / 错误态

| 状态 | 表现 |
|------|------|
| 未选精灵 | 卡片内「请搜索并选择…」；结果区引导文案 |
| 状态技能 | FULL 标明无直接伤害，不参与数值 |
| 幻系技能 | 星陨输入 disabled + 透明度降低 |

### 6.5 动效（克制）

- 过渡时长多为 **0.12–0.2s**（hover、Tab）  
- HP 条 **0.3s ease**  
- 交换按钮旋转 **0.2s**  
- 避免大面积动画，保证工具页性能  

---

## 7. 响应式断点

| 断点 | 布局变化 |
|------|----------|
| **> 800px** | FULL 结果双列 |
| **> 700px** | 攻防三列网格；特性双列；Tab 横排 |
| **≤ 700px** | 攻防纵向；交换居中；FULL 技能单列；Tab 竖排；倍率 2 列；header 精简；特性单列 |
| **≤ 800px** | FULL 结果单列 |

移动端优先保证：**能选精灵、能改关键数字、能读伤害**；次要说明可换行或收窄字号。

---

## 8. 无障碍与可用性建议

当前实现以功能为先，建议在迭代中对齐：

| 项 | 现状 / 建议 |
|----|-------------|
| 焦点 | 输入 focus 已有 accent 边；下拉建议支持键盘上下选择 |
| 对比度 | 暗色主文案良好；浅色属性 chip 已用深字 |
| 触控 | 按钮 ≥26px；移动端避免仅依赖 hover tooltip |
| 语义 | 结果大数字可加 `aria-live="polite"` 播报伤害变化 |
| 减动效 | 可尊重 `prefers-reduced-motion` |

---

## 9. 文案与图标约定

- **中文优先**，面向国内玩家  
- 面板用 emoji 作轻量图标：⚔️ 攻 / 🛡️ 防 / ⚡ 技能 / ⭐ 星陨·收藏 / 🔍 搜索 / 🌙 主题  
- 提示统一 `💡` 开头的浅色信息条  
- 免责、数据来源、QQ 反馈固定在 header 次要层级  

---

## 10. 技术实现约束（影响设计）

| 约束 | 对设计的影响 |
|------|----------------|
| 无框架、单 HTML 为主 | 组件以 class 约定复用，非设计系统包 |
| 样式集中在 `css/head.css` | 主题 = CSS 变量；改色优先改 token |
| 数据外置 `data/*.js` | UI 不硬编码图鉴内容；属性色可与 `TYPE_COLORS` 同步 |
| 图片来自 BWiki CDN | 头像失败需 fallback emoji/占位 |
| 无构建步骤 | 避免依赖图标库；emoji + CSS 即可 |

---

## 11. 页面级线框（桌面）

```
╔══════════════════════════════════════════════════════════╗
║ ⚔️  洛克王国 PVP 伤害计算器              [🌙 夜间]     ║
║     数据来源 · 免责 · 反馈                               ║
╠══════════════════════════════════════════════════════════╣
║  ┌─ 攻击方 ─────────┐  (⇄)  ┌─ 防御方 ─────────┐     ║
║  │ [搜索…………▼]      │        │ [搜索…………▼]      │     ║
║  │ ┌图┐ 名称 特性   │        │ ┌图┐ 名称 特性   │     ║
║  │ └─┘  [草]        │        │ └─┘  [火]        │     ║
║  │ 性格 [固执    ▼] │        │ 性格 [稳重    ▼] │     ║
║  │ [物攻*][魔攻][速]│        │ [物防*][魔防][速]│     ║
║  │ [ HP ][物防][魔防]│        │ [ HP ][物攻][魔攻]│     ║
║  │ 攻击能级 [−]0[+] │        │ 防御能级 [−]0[+] │     ║
║  └──────────────────┘        └──────────────────┘     ║
║  ┌ 技能参数 ──────────────────────────────────────┐   ║
║  │ [ 快捷A ][ 参数B ][ 完整FULL ]                   │   ║
║  │  …模式表单…                                      │   ║
║  │  [攻方特性面板] [防方特性面板]                    │   ║
║  └────────────────────────────────────────────────┘   ║
║  ┌ 结果 ─ accent 边 ──────────────────────────────┐   ║
║  │  精灵A → 精灵B                                  │   ║
║  │  1234 伤害  [克制×2] [本系]                     │   ║
║  │  ████████░░░░  62% HP · 大于半血                 │   ║
║  │  [拆解 chips…]                                  │   ║
║  │  formula: (atk/def)×37/41×…                     │   ║
║  └────────────────────────────────────────────────┘   ║
╚══════════════════════════════════════════════════════════╝
```

---

## 12. 设计验收清单

- [ ] 攻方始终橙系、防方始终蓝系，无交叉误用  
- [ ] 主题切换后卡片、输入、下拉、结果可读且无「白底白字」  
- [ ] 属性 chip 18 色与图鉴一致，浅色字用深色  
- [ ] 模式切换仅显示一个 section，Tab active 明确  
- [ ] 伤害数字 ≥ 结果区视觉重心；HP 条颜色阈值正确  
- [ ] ≤700px 可完成：选精灵 → 改 IV → 看结果  
- [ ] 焦点环可见；主按钮 hover 有反馈  
- [ ] 空状态有引导，不出现空白死板  

---

## 13. 相关文件索引

| 文件 | 设计相关内容 |
|------|----------------|
| `css/head.css` | 主题 token、布局、组件样式、响应式 |
| `index.html` | 结构、内联提示样式、结果 DOM 模板 |
| `data/type.js` | `TYPE_COLORS` / 属性色源 |
| `discus.html` | 讨论区独立页（更轻的 header + Giscus） |

---

## 14. 版本说明

| 项 | 内容 |
|----|------|
| 文档性质 | **现状设计说明**（as-built），非全新改版提案 |
| 对应实现 | RokePvP-Calculator 主站单页计算器 |
| 后续若改版 | 请先更新 token 与本文 §3–§5，再改 CSS，避免硬编码色值扩散 |

---

*文档生成自项目源码梳理，用于设计一致性维护与协作对齐。*
